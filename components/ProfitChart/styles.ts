import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
  },
  textDescription: {
    fontSize: 12,
    color: "#666",
    marginLeft: 5,
  },
  labelsContainer: {
    marginLeft: 120,
    marginTop: 10,
  },
});
